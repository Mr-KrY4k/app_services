# app_services

Плагин‑обёртка для удобной настройки Android‑проекта под GMS/HMS.

- **Единая CLI‑команда**: выбор между GMS, HMS и удалением настроек.
- **Безопасная очистка**: перед установкой всегда выполняется cleanup
  и для GMS, и для HMS.
- **Переиспользование логики**: используется уже готовый setup/cleanup
  из пакетов `gms_services` и `hms_services`.

---

## Установка

Добавьте зависимость в `pubspec.yaml` вашего приложения:

```yaml
dependencies:
  app_services:
    git:
      url: git@github.com:your-org/app_services.git
      ref: dev
```

Затем выполните:

```bash
flutter pub get
```

---

## Использование CLI

Запускать скрипт нужно **из корня Flutter‑проекта**, где есть директория
`android`.

### Базовая команда

```bash
dart run app_services:setup
```

Это то же самое, что:

```bash
dart run app_services
```

Откроется интерактивное меню:

```text
🔧 app_services: управление GMS/HMS‑настройками

Что вы хотите сделать?
  1) Установить GMS
  2) Установить HMS
  3) Удалить настройки (GMS и HMS)
  4) Ничего не делать / выход
```

Выберите нужный пункт, введя цифру и нажав Enter.

---

## Поведение скрипта

### Общие принципы

- Перед **любой установкой** (GMS или HMS) скрипт **сначала выполняет
  удаление настроек и GMS, и HMS**:
  - `cleanupHmsServices()` из `hms_services`.
  - `cleanupGmsServices()` из `gms_services`.
- Если при очистке возникают критичные ошибки (как в оригинальных
  bin‑скриптах этих пакетов), установка прерывается с кодом выхода `1`.

### 1) Установка GMS

Пункт `1` в меню:

1. Выполняется очистка HMS и GMS.
2. Запускается `setupGmsServices()` из пакета `gms_services`.
3. В консоль выводятся все сообщения из `SetupResult.messages`.
4. При ошибках – код выхода `1`, при успехе – сообщение
   «Установка GMS завершена успешно.»

### 2) Установка HMS

Пункт `2` в меню:

1. Выполняется очистка HMS и GMS.
2. Запускается `setupHmsServices()` из пакета `hms_services`.
3. В консоль выводятся все сообщения из `SetupResult.messages`.
4. При ошибках – код выхода `1`, при успехе – сообщение
   «Установка HMS завершена успешно.»

### 3) Удаление настроек (GMS и HMS)

Пункт `3` в меню:

1. Запускается `cleanupHmsServices()`.
2. Запускается `cleanupGmsServices()`.
3. В консоль выводятся все сообщения обоих вызовов.
4. При ошибках – код выхода `1`, при успехе – сообщение
   «Удаление настроек GMS и HMS завершено.»

### 4) Выход

Пункт `4` просто завершает скрипт без каких‑либо изменений.

---

## Примечания

- Скрипт ориентирован только на Android‑часть Flutter‑проекта.
- Запускать `dart run app_services:setup` нужно из корня проекта,
  где есть папка `android` (так же, как и оригинальные `setup/cleanup`
  из `gms_services` и `hms_services`).